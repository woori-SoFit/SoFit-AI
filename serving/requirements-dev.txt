# 테스트/개발 전용 의존성 (프로덕션 이미지에 포함되지 않음)
# 설치: pip install -r serving/requirements-dev.txt
-r requirements.txt

# ── 테스트 ──────────────────────────────────────────────────
pytest==8.3.5
pytest-cov==6.1.0
httpx==0.28.1         # FastAPI TestClient 의존성

# ── 정적 분석 (컨벤션: ruff 린트 + 포맷) ───────────────────
ruff==0.11.10
